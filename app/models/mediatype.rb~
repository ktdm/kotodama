class Mediatype < ActiveRecord::Base
  validates :title,
   :presence => true,
   :length => {:minimum => 1}
  serialize :signature, Array
  before_save :default_sig
  def default_sig
    self.signature = nil unless self.signature
  end
  #def to_param
  #  title #->url
  #end
end
